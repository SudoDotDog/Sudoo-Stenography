---
name: Recipe
---

import { Playground } from 'docz';
import { Recipe } from "../src";

# Recipe

<Playground>
  <Recipe
    interceptors={[{
        combo: ['a'],
        callback: () => console.log('a')
      }, {
        combo: ['ctrl+s'],
        callback: () => console.log('ctrl+s')
      }, {
        combo: ['alt+b'],
        callback: () => console.log('alt+b')
      }, {
        combo: ['shift+c'],
        callback: () => console.log('shift+c')
      }, {
        combo: ['up', 'up', 'down', 'down', 'left', 'right', 'left', 'right', 'b', 'a'],
        callback: () => console.log('WOW')
      }]}
  />
</Playground>
